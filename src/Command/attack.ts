// import {ICommand} from "./command";

// export class AttackCommand implements ICommand {
    // process(data: object, sessionId: string, ws: WebSocket.WebSocket): IResponse | undefined {
    //     return undefined;
    // }
// }